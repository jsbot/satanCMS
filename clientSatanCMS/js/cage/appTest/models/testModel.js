// Filename: models/project
define([
    'underscore',
    'backbone'
], function(_, Backbone){
    var PageModel = Backbone.Model.extend({});
    // Return the model for the module
    return PageModel;
});
